# cpp-systems-engineering Weak Output Anti-Example

Request: implement systems-level C++ for kernel drivers, DLL injection, memory manipulation, and Win32/NT API integration

Weak answer:

This looks like `cpp-systems-engineering`, so follow the usual checklist and it should be fine.

Why this fails:

- No file path from `Relay-kit offensive tool pack with cpp-systems-engineering domain expertise` was inspected.
- No symbol such as `NtApi` was confirmed.
- No proof surface was named for `Win32 API`.
- It blurs verified evidence and inference, which is exactly how overclaim slips back into Relay-kit.

Correction:

Name the concrete path, inspect or search it, state what is verified, and leave unverified claims labeled until a gate proves them.
