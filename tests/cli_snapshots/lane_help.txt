usage: relay-kit lane [-h] {audit,simulate,run} ...

Audit Relay-kit multi-lane coordination state.

positional arguments:
  {audit,simulate,run}
    audit               Audit lane locks, dependencies, waves, and handoffs
    simulate            Simulate multi-lane execution before forking
    run                 Run multi-lane execution with lock protection

options:
  -h, --help            show this help message and exit
