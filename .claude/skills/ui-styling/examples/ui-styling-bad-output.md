# ui-styling Weak Output Anti-Example

Request: handle a ui-styling request, name files first, and identify the proof surface before editing

Weak answer:

This looks like `ui-styling`, so follow the usual checklist and it should be fine.

Why this fails:

- No file path from `design-system / styling repo with tokens, themes, and reusable components` was inspected.
- No symbol such as `designTokens` was confirmed.
- No proof surface was named for `design token`.
- It blurs verified evidence and inference, which is exactly how overclaim slips back into Relay-kit.

Correction:

Name the concrete path, inspect or search it, state what is verified, and leave unverified claims labeled until a gate proves them.
