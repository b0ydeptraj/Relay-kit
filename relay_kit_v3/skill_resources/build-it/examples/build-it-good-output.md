# build-it Battle-Calibrated Output

Request: implement an approved story or tech spec with controlled scope and evidence

Recommended skill: `build-it` because the request matches `specialist` work and has concrete repo anchors.

Read first:

- `relay_kit_v3/registry/skills.py`
- `relay_kit_public_cli.py`

Evidence gathered:

- Confirmed `developer` or nearby ownership before recommending changes.
- Checked `story` and `tech-spec` against the relevant source path.
- Identified `implementation` as a required proof term before completion.

Answer:

The safe next move is to inspect the named file path, compare it with the expected test or docs surface, and only then choose implementation, review, or planning. If the anchor is missing, ask one question that names the missing file, PR, log, screen, or workflow.

Residual risk:

- `controlled scope` remains unverified until the focused gate or benchmark hit is captured.
