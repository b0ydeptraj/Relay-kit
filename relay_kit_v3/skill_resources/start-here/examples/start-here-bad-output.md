# start-here Weak Output Anti-Example

Request: route a request to the right Relay-kit path, next skill, and artifact without guessing

Weak answer:

This looks like `start-here`, so follow the usual checklist and it should be fine.

Why this fails:

- No file path from `Relay-kit offensive tool pack with start-here domain expertise` was inspected.
- No symbol such as `workflow_router` was confirmed.
- No proof surface was named for `routing`.
- It blurs verified evidence and inference, which is exactly how overclaim slips back into Relay-kit.

Correction:

Name the concrete path, inspect or search it, state what is verified, and leave unverified claims labeled until a gate proves them.
