# process-injection-techniques Weak Output Anti-Example

Request: implement process injection techniques including DLL injection, process hollowing, APC injection, and thread hijacking

Weak answer:

This looks like `process-injection-techniques`, so follow the usual checklist and it should be fine.

Why this fails:

- No file path from `Relay-kit offensive tool pack with process-injection-techniques domain expertise` was inspected.
- No symbol such as `inject_dll` was confirmed.
- No proof surface was named for `DLL injection`.
- It blurs verified evidence and inference, which is exactly how overclaim slips back into Relay-kit.

Correction:

Name the concrete path, inspect or search it, state what is verified, and leave unverified claims labeled until a gate proves them.
