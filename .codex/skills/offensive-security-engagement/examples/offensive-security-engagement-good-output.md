# offensive-security-engagement Battle-Calibrated Output

Request: gate and provide context for offensive security lanes including engagement proof caching and ATT&CK mapping

Recommended skill: `offensive-security-engagement` because the request matches `engagement-gate` work and has concrete repo anchors.

Read first:

- `relay_kit_v3/registry/skills.py`

Evidence gathered:

- Confirmed `engagement_proof` or nearby ownership before recommending changes.
- Checked `engagement proof` and `authorization` against the relevant source path.
- Identified `ATT&CK phase` as a required proof term before completion.

Answer:

The safe next move is to inspect the named file path, compare it with the expected test or docs surface, and only then choose implementation, review, or planning. If the anchor is missing, ask one question that names the missing file, PR, log, screen, or workflow.

Residual risk:

- `risk tier` remains unverified until the focused gate or benchmark hit is captured.
