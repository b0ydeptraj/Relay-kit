# advanced-python-engineering Battle-Calibrated Output

Request: implement advanced Python patterns including async I/O, ctypes/cffi interop, metaprogramming, and C extension modules

Recommended skill: `advanced-python-engineering` because the request matches `python-specialist` work and has concrete repo anchors.

Read first:

- `relay_kit_v3/registry/skills.py`
- `relay_kit_public_cli.py`

Evidence gathered:

- Confirmed `asyncio` or nearby ownership before recommending changes.
- Checked `async model` and `ctypes signature` against the relevant source path.
- Identified `error hierarchy` as a required proof term before completion.

Answer:

The safe next move is to inspect the named file path, compare it with the expected test or docs surface, and only then choose implementation, review, or planning. If the anchor is missing, ask one question that names the missing file, PR, log, screen, or workflow.

Residual risk:

- `version pin` remains unverified until the focused gate or benchmark hit is captured.
