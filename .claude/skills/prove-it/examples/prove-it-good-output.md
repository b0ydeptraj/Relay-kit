# prove-it Battle-Calibrated Output

Request: verify completion claims with one last evidence pass before calling work done

Recommended skill: `prove-it` because the request matches `specialist` work and has concrete repo anchors.

Read first:

- `relay_kit_v3/registry/skills.py`
- `relay_kit_public_cli.py`

Evidence gathered:

- Confirmed `evidence_before_completion` or nearby ownership before recommending changes.
- Checked `evidence` and `claim` against the relevant source path.
- Identified `proof` as a required proof term before completion.

Answer:

The safe next move is to inspect the named file path, compare it with the expected test or docs surface, and only then choose implementation, review, or planning. If the anchor is missing, ask one question that names the missing file, PR, log, screen, or workflow.

Residual risk:

- `residual risk` remains unverified until the focused gate or benchmark hit is captured.
