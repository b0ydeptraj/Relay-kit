# desktop-python-ui Battle-Calibrated Output

Request: build desktop applications using Python UI frameworks like PyQt, PySide, Tkinter, or Dear PyGui

Recommended skill: `desktop-python-ui` because the request matches `python-ui-developer` work and has concrete repo anchors.

Read first:

- `relay_kit_v3/registry/skills.py`

Evidence gathered:

- Confirmed `QApplication` or nearby ownership before recommending changes.
- Checked `event loop` and `signal-slot` against the relevant source path.
- Identified `widget tree` as a required proof term before completion.

Answer:

The safe next move is to inspect the named file path, compare it with the expected test or docs surface, and only then choose implementation, review, or planning. If the anchor is missing, ask one question that names the missing file, PR, log, screen, or workflow.

Residual risk:

- `threading model` remains unverified until the focused gate or benchmark hit is captured.
