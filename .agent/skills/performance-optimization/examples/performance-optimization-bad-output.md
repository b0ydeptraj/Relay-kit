# performance-optimization Weak Output Anti-Example

Request: handle a performance-optimization request, name files first, and identify the proof surface before editing

Weak answer:

This looks like `performance-optimization`, so follow the usual checklist and it should be fine.

Why this fails:

- No file path from `service repo with a slow endpoint, a database layer, and a benchmark or load-test harness` was inspected.
- No symbol such as `search_handler` was confirmed.
- No proof surface was named for `baseline p95`.
- It blurs verified evidence and inference, which is exactly how overclaim slips back into Relay-kit.

Correction:

Name the concrete path, inspect or search it, state what is verified, and leave unverified claims labeled until a gate proves them.
