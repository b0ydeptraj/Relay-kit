# antibot-challenge-solving Battle-Calibrated Output

Request: solve or bypass antibot challenges including CAPTCHAs, JavaScript challenges, and behavioral verification

Recommended skill: `antibot-challenge-solving` because the request matches `antibot-specialist` work and has concrete repo anchors.

Read first:

- `relay_kit_v3/registry/skills.py`

Evidence gathered:

- Confirmed `challenge_solver` or nearby ownership before recommending changes.
- Checked `challenge type` and `solver strategy` against the relevant source path.
- Identified `behavioral bypass` as a required proof term before completion.

Answer:

The safe next move is to inspect the named file path, compare it with the expected test or docs surface, and only then choose implementation, review, or planning. If the anchor is missing, ask one question that names the missing file, PR, log, screen, or workflow.

Residual risk:

- `token extraction` remains unverified until the focused gate or benchmark hit is captured.
