# frontend-crypto-reverse Battle-Calibrated Output

Request: reverse client-side cryptography, obfuscated JavaScript, WASM modules, and API signing parameters

Recommended skill: `frontend-crypto-reverse` because the request matches `crypto-reverser` work and has concrete repo anchors.

Read first:

- `relay_kit_v3/registry/skills.py`

Evidence gathered:

- Confirmed `crypto_params` or nearby ownership before recommending changes.
- Checked `JS deobfuscation` and `WASM reverse` against the relevant source path.
- Identified `signing algorithm` as a required proof term before completion.

Answer:

The safe next move is to inspect the named file path, compare it with the expected test or docs surface, and only then choose implementation, review, or planning. If the anchor is missing, ask one question that names the missing file, PR, log, screen, or workflow.

Residual risk:

- `key extraction` remains unverified until the focused gate or benchmark hit is captured.
