# windows-native-internals Weak Output Anti-Example

Request: work with Windows NT internals including undocumented APIs, PEB/TEB structures, kernel objects, and system information classes

Weak answer:

This looks like `windows-native-internals`, so follow the usual checklist and it should be fine.

Why this fails:

- No file path from `Relay-kit offensive tool pack with windows-native-internals domain expertise` was inspected.
- No symbol such as `PEB` was confirmed.
- No proof surface was named for `PEB walk`.
- It blurs verified evidence and inference, which is exactly how overclaim slips back into Relay-kit.

Correction:

Name the concrete path, inspect or search it, state what is verified, and leave unverified claims labeled until a gate proves them.
