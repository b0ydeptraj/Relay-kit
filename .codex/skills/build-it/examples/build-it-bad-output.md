# build-it Weak Output Anti-Example

Request: implement an approved story or tech spec with controlled scope and evidence

Weak answer:

This looks like `build-it`, so follow the usual checklist and it should be fine.

Why this fails:

- No file path from `Relay-kit offensive tool pack with build-it domain expertise` was inspected.
- No symbol such as `developer` was confirmed.
- No proof surface was named for `story`.
- It blurs verified evidence and inference, which is exactly how overclaim slips back into Relay-kit.

Correction:

Name the concrete path, inspect or search it, state what is verified, and leave unverified claims labeled until a gate proves them.
