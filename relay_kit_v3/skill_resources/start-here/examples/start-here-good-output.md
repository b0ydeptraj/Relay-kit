# start-here Battle-Calibrated Output

Request: route a request to the right Relay-kit path, next skill, and artifact without guessing

Recommended skill: `start-here` because the request matches `specialist` work and has concrete repo anchors.

Read first:

- `relay_kit_v3/registry/skills.py`
- `relay_kit_public_cli.py`

Evidence gathered:

- Confirmed `workflow_router` or nearby ownership before recommending changes.
- Checked `routing` and `next skill` against the relevant source path.
- Identified `track` as a required proof term before completion.

Answer:

The safe next move is to inspect the named file path, compare it with the expected test or docs surface, and only then choose implementation, review, or planning. If the anchor is missing, ask one question that names the missing file, PR, log, screen, or workflow.

Residual risk:

- `workflow state` remains unverified until the focused gate or benchmark hit is captured.
