# aesthetic Weak Output Anti-Example

Request: handle a aesthetic request, name files first, and identify the proof surface before editing

Weak answer:

This looks like `aesthetic`, so follow the usual checklist and it should be fine.

Why this fails:

- No file path from `product UI repo where first-pass output risks looking generic / obviously AI-generated` was inspected.
- No symbol such as `Hero` was confirmed.
- No proof surface was named for `reference screenshot`.
- It blurs verified evidence and inference, which is exactly how overclaim slips back into Relay-kit.

Correction:

Name the concrete path, inspect or search it, state what is verified, and leave unverified claims labeled until a gate proves them.
