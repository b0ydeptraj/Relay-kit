# binary-stealth-obfuscation Battle-Calibrated Output

Request: obfuscate binary payloads to evade static analysis, AV signatures, and YARA rules

Recommended skill: `binary-stealth-obfuscation` because the request matches `obfuscation-specialist` work and has concrete repo anchors.

Read first:

- `relay_kit_v3/registry/skills.py`

Evidence gathered:

- Confirmed `obfuscator` or nearby ownership before recommending changes.
- Checked `entropy control` and `signature evasion` against the relevant source path.
- Identified `packing` as a required proof term before completion.

Answer:

The safe next move is to inspect the named file path, compare it with the expected test or docs surface, and only then choose implementation, review, or planning. If the anchor is missing, ask one question that names the missing file, PR, log, screen, or workflow.

Residual risk:

- `mutation engine` remains unverified until the focused gate or benchmark hit is captured.
