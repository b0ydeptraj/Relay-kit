# mmo-llm-automation Battle-Calibrated Output

Request: integrate LLM APIs for bulk content generation, behavioral variance, and Sybil evasion in MMO operations

Recommended skill: `mmo-llm-automation` because the request matches `llm-automation-specialist` work and has concrete repo anchors.

Read first:

- `relay_kit_v3/registry/skills.py`

Evidence gathered:

- Confirmed `llm_client` or nearby ownership before recommending changes.
- Checked `prompt diversity` and `behavioral variance` against the relevant source path.
- Identified `content generation` as a required proof term before completion.

Answer:

The safe next move is to inspect the named file path, compare it with the expected test or docs surface, and only then choose implementation, review, or planning. If the anchor is missing, ask one question that names the missing file, PR, log, screen, or workflow.

Residual risk:

- `Sybil evasion` remains unverified until the focused gate or benchmark hit is captured.
