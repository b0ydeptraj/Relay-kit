# technical-writing Weak Output Anti-Example

Request: handle a technical-writing request, name files first, and identify the proof surface before editing

Weak answer:

This looks like `technical-writing`, so follow the usual checklist and it should be fine.

Why this fails:

- No file path from `repo with a CLI or library, an outdated README, and example scripts` was inspected.
- No symbol such as `main` was confirmed.
- No proof surface was named for `verified command`.
- It blurs verified evidence and inference, which is exactly how overclaim slips back into Relay-kit.

Correction:

Name the concrete path, inspect or search it, state what is verified, and leave unverified claims labeled until a gate proves them.
