usage: relay-kit command list [-h] [--json] [project_path]

positional arguments:
  project_path  Project root (reserved for CLI symmetry)

options:
  -h, --help    show this help message and exit
  --json        Emit machine-readable lifecycle command list
