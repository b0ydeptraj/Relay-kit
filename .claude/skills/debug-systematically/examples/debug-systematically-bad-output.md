# debug-systematically Weak Output Anti-Example

Request: debug a bug, regression, or mismatch using disciplined root-cause analysis

Weak answer:

This looks like `debug-systematically`, so follow the usual checklist and it should be fine.

Why this fails:

- No file path from `Relay-kit offensive tool pack with debug-systematically domain expertise` was inspected.
- No symbol such as `debug_hub` was confirmed.
- No proof surface was named for `root cause`.
- It blurs verified evidence and inference, which is exactly how overclaim slips back into Relay-kit.

Correction:

Name the concrete path, inspect or search it, state what is verified, and leave unverified claims labeled until a gate proves them.
