# terminal-operator-ui Battle-Calibrated Output

Request: build terminal-based operator UIs using curses, Rich, Textual, or similar TUI frameworks

Recommended skill: `terminal-operator-ui` because the request matches `tui-developer` work and has concrete repo anchors.

Read first:

- `relay_kit_v3/registry/skills.py`

Evidence gathered:

- Confirmed `App` or nearby ownership before recommending changes.
- Checked `TUI framework` and `keybinding` against the relevant source path.
- Identified `live update` as a required proof term before completion.

Answer:

The safe next move is to inspect the named file path, compare it with the expected test or docs surface, and only then choose implementation, review, or planning. If the anchor is missing, ask one question that names the missing file, PR, log, screen, or workflow.

Residual risk:

- `responsive layout` remains unverified until the focused gate or benchmark hit is captured.
