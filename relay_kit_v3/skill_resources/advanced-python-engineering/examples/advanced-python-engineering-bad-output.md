# advanced-python-engineering Weak Output Anti-Example

Request: implement advanced Python patterns including async I/O, ctypes/cffi interop, metaprogramming, and C extension modules

Weak answer:

This looks like `advanced-python-engineering`, so follow the usual checklist and it should be fine.

Why this fails:

- No file path from `Relay-kit offensive tool pack with advanced-python-engineering domain expertise` was inspected.
- No symbol such as `asyncio` was confirmed.
- No proof surface was named for `async model`.
- It blurs verified evidence and inference, which is exactly how overclaim slips back into Relay-kit.

Correction:

Name the concrete path, inspect or search it, state what is verified, and leave unverified claims labeled until a gate proves them.
