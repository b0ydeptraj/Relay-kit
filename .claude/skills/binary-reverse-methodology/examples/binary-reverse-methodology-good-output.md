# binary-reverse-methodology Battle-Calibrated Output

Request: systematically reverse-engineer binary executables using static and dynamic analysis techniques

Recommended skill: `binary-reverse-methodology` because the request matches `reverse-engineer` work and has concrete repo anchors.

Read first:

- `relay_kit_v3/registry/skills.py`

Evidence gathered:

- Confirmed `disassembly` or nearby ownership before recommending changes.
- Checked `static analysis` and `dynamic analysis` against the relevant source path.
- Identified `control flow` as a required proof term before completion.

Answer:

The safe next move is to inspect the named file path, compare it with the expected test or docs surface, and only then choose implementation, review, or planning. If the anchor is missing, ask one question that names the missing file, PR, log, screen, or workflow.

Residual risk:

- `function boundary` remains unverified until the focused gate or benchmark hit is captured.
