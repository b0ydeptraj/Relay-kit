# mmo-onchain-security-audit Weak Output Anti-Example

Request: audit on-chain scripts, smart contracts, and wallet automation for security vulnerabilities before deployment

Weak answer:

This looks like `mmo-onchain-security-audit`, so follow the usual checklist and it should be fine.

Why this fails:

- No file path from `Relay-kit offensive tool pack with mmo-onchain-security-audit domain expertise` was inspected.
- No symbol such as `contract_audit` was confirmed.
- No proof surface was named for `reentrancy`.
- It blurs verified evidence and inference, which is exactly how overclaim slips back into Relay-kit.

Correction:

Name the concrete path, inspect or search it, state what is verified, and leave unverified claims labeled until a gate proves them.
