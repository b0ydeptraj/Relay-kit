# write-steps Battle-Calibrated Output

Request: slice approved work into small, buildable, verifiable implementation steps

Recommended skill: `write-steps` because the request matches `specialist` work and has concrete repo anchors.

Read first:

- `relay_kit_v3/registry/skills.py`
- `relay_kit_public_cli.py`

Evidence gathered:

- Confirmed `scrum_master` or nearby ownership before recommending changes.
- Checked `implementation steps` and `verifiable` against the relevant source path.
- Identified `buildable` as a required proof term before completion.

Answer:

The safe next move is to inspect the named file path, compare it with the expected test or docs surface, and only then choose implementation, review, or planning. If the anchor is missing, ask one question that names the missing file, PR, log, screen, or workflow.

Residual risk:

- `done criteria` remains unverified until the focused gate or benchmark hit is captured.
