# aesthetic Battle-Calibrated Output

Request: handle a aesthetic request, name files first, and identify the proof surface before editing

Recommended skill: `aesthetic` because the request matches `aesthetic-review` work and has concrete repo anchors.

Read first:

- `src/app/(marketing)/page.tsx`
- `src/components/Hero.tsx`
- `tailwind.config.ts`

Evidence gathered:

- Confirmed `Hero` ownership before recommending changes.
- Checked `reference screenshot` and `spacing scale` against the relevant source path.
- Identified `visual regression` as a required proof term before completion.

Answer:

The safe next move is to inspect the named path, compare it with the expected test, metric, config, or docs surface, and only then choose implementation, review, or planning. If the anchor is missing, ask one question that names the missing file, PR, log, screen, or workflow.

Residual risk:

- `type hierarchy` remains unverified until the focused gate or proof surface is captured.
