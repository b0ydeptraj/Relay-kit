# frontend-design Weak Output Anti-Example

Request: handle a frontend-design request, name files first, and identify the proof surface before editing

Weak answer:

This looks like `frontend-design`, so follow the usual checklist and it should be fine.

Why this fails:

- No file path from `component/page repo where the user asks to build UI and visual quality matters` was inspected.
- No symbol such as `LoginForm` was confirmed.
- No proof surface was named for `component boundary`.
- It blurs verified evidence and inference, which is exactly how overclaim slips back into Relay-kit.

Correction:

Name the concrete path, inspect or search it, state what is verified, and leave unverified claims labeled until a gate proves them.
