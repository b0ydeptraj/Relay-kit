# desktop-imgui-development Weak Output Anti-Example

Request: build desktop overlay and tool UIs using Dear ImGui with DirectX/OpenGL backends

Weak answer:

This looks like `desktop-imgui-development`, so follow the usual checklist and it should be fine.

Why this fails:

- No file path from `Relay-kit offensive tool pack with desktop-imgui-development domain expertise` was inspected.
- No symbol such as `ImGui` was confirmed.
- No proof surface was named for `render loop`.
- It blurs verified evidence and inference, which is exactly how overclaim slips back into Relay-kit.

Correction:

Name the concrete path, inspect or search it, state what is verified, and leave unverified claims labeled until a gate proves them.
