# mobile-app-reverse Weak Output Anti-Example

Request: reverse-engineer mobile applications including APK/IPA analysis, SSL pinning bypass, and API extraction

Weak answer:

This looks like `mobile-app-reverse`, so follow the usual checklist and it should be fine.

Why this fails:

- No file path from `Relay-kit offensive tool pack with mobile-app-reverse domain expertise` was inspected.
- No symbol such as `apk_decompile` was confirmed.
- No proof surface was named for `APK decompile`.
- It blurs verified evidence and inference, which is exactly how overclaim slips back into Relay-kit.

Correction:

Name the concrete path, inspect or search it, state what is verified, and leave unverified claims labeled until a gate proves them.
