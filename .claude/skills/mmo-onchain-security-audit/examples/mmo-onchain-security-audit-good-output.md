# mmo-onchain-security-audit Battle-Calibrated Output

Request: audit on-chain scripts, smart contracts, and wallet automation for security vulnerabilities before deployment

Recommended skill: `mmo-onchain-security-audit` because the request matches `onchain-auditor` work and has concrete repo anchors.

Read first:

- `relay_kit_v3/registry/skills.py`

Evidence gathered:

- Confirmed `contract_audit` or nearby ownership before recommending changes.
- Checked `reentrancy` and `front-running` against the relevant source path.
- Identified `access control` as a required proof term before completion.

Answer:

The safe next move is to inspect the named file path, compare it with the expected test or docs surface, and only then choose implementation, review, or planning. If the anchor is missing, ask one question that names the missing file, PR, log, screen, or workflow.

Residual risk:

- `integer overflow` remains unverified until the focused gate or benchmark hit is captured.
