# network-stealth-c2 Battle-Calibrated Output

Request: build and operate C2 infrastructure with traffic blending, domain fronting, protocol mimicry, and beaconing discipline

Recommended skill: `network-stealth-c2` because the request matches `c2-engineer` work and has concrete repo anchors.

Read first:

- `relay_kit_v3/registry/skills.py`

Evidence gathered:

- Confirmed `beacon_config` or nearby ownership before recommending changes.
- Checked `domain fronting` and `traffic blending` against the relevant source path.
- Identified `jitter` as a required proof term before completion.

Answer:

The safe next move is to inspect the named file path, compare it with the expected test or docs surface, and only then choose implementation, review, or planning. If the anchor is missing, ask one question that names the missing file, PR, log, screen, or workflow.

Residual risk:

- `redirector chain` remains unverified until the focused gate or benchmark hit is captured.
