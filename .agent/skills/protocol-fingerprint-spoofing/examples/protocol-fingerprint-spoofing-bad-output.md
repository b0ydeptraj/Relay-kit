# protocol-fingerprint-spoofing Weak Output Anti-Example

Request: spoof TLS/HTTP protocol fingerprints to match specific clients and evade JA3/JA4/HTTP2 fingerprinting

Weak answer:

This looks like `protocol-fingerprint-spoofing`, so follow the usual checklist and it should be fine.

Why this fails:

- No file path from `Relay-kit offensive tool pack with protocol-fingerprint-spoofing domain expertise` was inspected.
- No symbol such as `ja3_hash` was confirmed.
- No proof surface was named for `JA3 fingerprint`.
- It blurs verified evidence and inference, which is exactly how overclaim slips back into Relay-kit.

Correction:

Name the concrete path, inspect or search it, state what is verified, and leave unverified claims labeled until a gate proves them.
