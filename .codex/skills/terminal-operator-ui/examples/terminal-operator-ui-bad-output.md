# terminal-operator-ui Weak Output Anti-Example

Request: build terminal-based operator UIs using curses, Rich, Textual, or similar TUI frameworks

Weak answer:

This looks like `terminal-operator-ui`, so follow the usual checklist and it should be fine.

Why this fails:

- No file path from `Relay-kit offensive tool pack with terminal-operator-ui domain expertise` was inspected.
- No symbol such as `App` was confirmed.
- No proof surface was named for `TUI framework`.
- It blurs verified evidence and inference, which is exactly how overclaim slips back into Relay-kit.

Correction:

Name the concrete path, inspect or search it, state what is verified, and leave unverified claims labeled until a gate proves them.
