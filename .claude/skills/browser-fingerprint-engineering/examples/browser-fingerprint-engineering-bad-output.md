# browser-fingerprint-engineering Weak Output Anti-Example

Request: engineer consistent browser fingerprints to defeat canvas, WebGL, AudioContext, and behavioral fingerprinting

Weak answer:

This looks like `browser-fingerprint-engineering`, so follow the usual checklist and it should be fine.

Why this fails:

- No file path from `Relay-kit offensive tool pack with browser-fingerprint-engineering domain expertise` was inspected.
- No symbol such as `canvas_fingerprint` was confirmed.
- No proof surface was named for `canvas hash`.
- It blurs verified evidence and inference, which is exactly how overclaim slips back into Relay-kit.

Correction:

Name the concrete path, inspect or search it, state what is verified, and leave unverified claims labeled until a gate proves them.
