# desktop-python-ui Weak Output Anti-Example

Request: build desktop applications using Python UI frameworks like PyQt, PySide, Tkinter, or Dear PyGui

Weak answer:

This looks like `desktop-python-ui`, so follow the usual checklist and it should be fine.

Why this fails:

- No file path from `Relay-kit offensive tool pack with desktop-python-ui domain expertise` was inspected.
- No symbol such as `QApplication` was confirmed.
- No proof surface was named for `event loop`.
- It blurs verified evidence and inference, which is exactly how overclaim slips back into Relay-kit.

Correction:

Name the concrete path, inspect or search it, state what is verified, and leave unverified claims labeled until a gate proves them.
