# field-journal-evolution Battle-Calibrated Output

Request: evolve field journal entries by capturing pitfalls, techniques, and routing gaps with evidence backing

Recommended skill: `field-journal-evolution` because the request matches `journal-curator` work and has concrete repo anchors.

Read first:

- `relay_kit_v3/field_journal.py`
- `relay_kit_v3/evidence_quality.py`

Evidence gathered:

- Confirmed `capture_entry` or nearby ownership before recommending changes.
- Checked `evidence ref` and `confidence level` against the relevant source path.
- Identified `pattern capture` as a required proof term before completion.

Answer:

The safe next move is to inspect the named file path, compare it with the expected test or docs surface, and only then choose implementation, review, or planning. If the anchor is missing, ask one question that names the missing file, PR, log, screen, or workflow.

Residual risk:

- `journal append` remains unverified until the focused gate or benchmark hit is captured.
