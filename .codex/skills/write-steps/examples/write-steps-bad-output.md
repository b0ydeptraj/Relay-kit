# write-steps Weak Output Anti-Example

Request: slice approved work into small, buildable, verifiable implementation steps

Weak answer:

This looks like `write-steps`, so follow the usual checklist and it should be fine.

Why this fails:

- No file path from `Relay-kit offensive tool pack with write-steps domain expertise` was inspected.
- No symbol such as `scrum_master` was confirmed.
- No proof surface was named for `implementation steps`.
- It blurs verified evidence and inference, which is exactly how overclaim slips back into Relay-kit.

Correction:

Name the concrete path, inspect or search it, state what is verified, and leave unverified claims labeled until a gate proves them.
